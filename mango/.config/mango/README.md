# Dependencies for my mango config

1. waybar: status bar
2. swaync: notification daemon
3. swaybg: background manager

NOTE: It is required to make `autostart.sh` an executable with `chmod +x autostart.sh`
