1. Run `install_packages.sh`
2. Install Homebrew and wire it into `.bashrc` with `bash install/optional/homebrew/enable.sh`
3. Run `install_brewfile.sh`

(Note: Yes, I'm using homebrew on ubuntu because apt is often not up-to-date with their packages and does not have many to begin with)